## Web map data overlays

This folder is for **small, web-friendly** artifacts produced by `analysis/`:

- `aoi_risk_latest.geojson`: per-AOI night heat risk score + supporting fields

Keep this folder lightweight (GeoJSON/CSV) so it can be served statically by the frontend.

